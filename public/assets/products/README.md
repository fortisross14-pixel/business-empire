# Product illustration assets

Each file is a static product-family illustration used as the hero art inside product cards. Brand and IP identity are UI overlays, so art can be replaced independently. Keep the 512×360 viewBox and generous outer padding.
